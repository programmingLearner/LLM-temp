# Define the maximum length for the file name
max_length = 255

# Suppose you have a long file name
long_file_name = "This_is_a_very_long_file_name_with_many_characters.txt"

# Truncate the file name to ensure it doesn’t exceed the maximum length
# Ensure to keep the file extension intact
base_name, extension = os.path.splitext(long_file_name)
truncated_name = base_name[:max_length - len(extension)] + extension

print(truncated_name)

#!/bin/bash

prefix="./some/path/"

for dir in ${prefix}zeroed*/; do
    if [ -d "$dir" ]; then
        # Execute your Python script with the directory name as an argument
        python3 your_script.py "$dir"
    fi
done
